const Loader = () => (
  <div>
    <img
      src="/assets/icons/loading.png"
      alt="loader"
      width={20}
      height={20}
      className="animate-spin"
    />
  </div>
);

export default Loader;
